@page
@model RazorPages.Pages.Anions.IndexModel
@{
    Thread.CurrentThread.CurrentCulture = new System.Globalization.CultureInfo(HttpContext.Request.Cookies["Culture"] ?? "en-GB");
}
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Anion[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Anion[0].Symbol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Anion[0].Concentration)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Anion) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Symbol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Concentration)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-Id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-Id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-Id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
